{
  "name": "genesis-brighton-gpu-interface",
  "version": "1.0.0",
  "description": "API contract + adapter between Brighton Protocol and GPU acceleration (RAPIDS cuML/cuGraph). Runs CPU-side, translates Brighton data into GPU-ready batch format.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.5",
    "@types/express": "^4.17.21"
  }
}
